<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="beauty">
    
	<select id="getBeatyList" parameterType="map" resultType="map">  
	    select num, title, loc 
	    from beauty
	</select>
	
	<select id="getBeatyListByName" parameterType="map" resultType="map">  
	    select num, title, loc 
	    from beauty 
	    where title like '%${beautyTitle}%'
	</select>
	
	<update id="setUsersBeautyById" parameterType="map">  
	    update user set beautyNum = #{beautyNum} 
	    where id =  #{id}
	</update>
	
	<update id="updateNewPhone_id" parameterType="map">  
	    update beauty set master_phone_id = #{phone_id} 
	    where num =  #{beautyNum}
	</update>
	
	<select id="BookingList_for_user_GET" parameterType="map" resultType="map">  
	    select num, beauty_title, hair_style, booking_date, booking_time, memo 
	    from booking
	    where user_id = #{id} AND isDone='1'
	</select>
	
	
	<update id="setting_beauty" parameterType="map">  
	    update beauty set working_time_from = #{working_time_from}, working_time_to = #{working_time_to}, loc = #{loc}, booking_method = #{booking_method}, booking_delay_time = #{booking_delay_time}, booking_delay_time_for_cut = #{booking_delay_time_for_cut}, dayOff = #{booking_dayOff}, isNotice = #{isNotice}, notice = #{notice} 
	    where num =  #{num}
	</update>
	
	
	
</mapper>
<!-- 
USERNO INT GENERATED BY DEFAULT AS IDENTITY 
          (START WITH 1, INCREMENT BY 1) NOT NULL,
    USERID VARCHAR(45) NOT NULL,
	USERNAME VARCHAR(45) NOT NULL,
	PASSWORD VARCHAR(45) NOT NULL,
 -->